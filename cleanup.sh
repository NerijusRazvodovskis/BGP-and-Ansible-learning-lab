#!/bin/bash

ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.1"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.2"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.3"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.4"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.5"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.6"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.7"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.8"
ssh-keygen -f "$HOME/.ssh/known_hosts" -R "172.20.20.9"
